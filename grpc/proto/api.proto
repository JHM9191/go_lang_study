syntax = "proto3";

package grpc.proto;
option go_package = "grpc/proto";

service Api {
  rpc GetHello(Request) returns (Reply);
}

message Request {
  string name = 1;
}

message Reply {
  string message = 1;
}